# vim: foldmethod=marker:foldlevel=0 textwidth=80 wrap syn=sh
alias media="cd /run/media/MrDeath/media"

# force 256 colour mode, so we get the correct output in i.e. Vim
alias tmux="tmux -2"

alias igrep="grep -i"

alias cinit='eval "$(chef shell-init "$(basename $SHELL)")"'

alias ytoj='ruby -ryaml -rjson -e "puts(YAML.load(ARGF.read).to_json)"'
alias jtoy='ruby -ryaml -rjson -e "puts(JSON.load(ARGF.read).to_yaml)"'

alias cinit='eval "$(chef shell-init "$(basename $SHELL)")"'
