#!/usr/bin/env bash
root="$(git rev-parse --show-toplevel)"
while [[ "$PWD" != "$root" ]]; do
  if [[ -f "gradlew" ]]; then
    "./gradlew" "$@"
  fi
  cd ..
done

if [[ -f "gradlew" ]]; then
  "./gradlew" "$@"
fi

exit 1
