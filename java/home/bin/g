#!/usr/bin/env bash
root="$(git rev-parse --show-toplevel)"
while [[ "$PWD" != "$root" ]]; do
  if [[ -f "gradlew" ]]; then
    "./gradlew" "$@"
    exit $?
  fi
  cd ..
done

if [[ -f "gradlew" ]]; then
  "./gradlew" "$@"
  exit $?
else
  exit 1
fi

