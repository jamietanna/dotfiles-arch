#!/usr/bin/env ruby
status = `pactl get-source-mute '@DEFAULT_SOURCE@'`.strip
input = status.split(':')[1].strip
muted = input == 'yes'
out = ""
if muted
  out += "%{B#cc241d}"
  out += "%{F#ebdbb2}"
  out += " "

  out += ""

  out += " "
else
  out += " "

  out += ""

  out += " "
end

out += "%{F-}"

puts out
